<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="10dp">

    <!-- Z=0 (back): drifting particle field — AVD, loops forever, theme-tinted at runtime -->
    <ImageView
        android:id="@+id/iv_particle_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:scaleType="fitXY"
        android:src="@drawable/widget_particles_avd"
        android:contentDescription="@null" />

    <!-- Z=1: breathing border glow — AVD, loops forever, theme-tinted at runtime -->
    <ImageView
        android:id="@+id/iv_border_glow"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:scaleType="fitXY"
        android:src="@drawable/widget_glow_avd"
        android:contentDescription="@null" />

    <!-- TOP ROW: coin symbol + 24h change + refresh button -->
    <LinearLayout
        android:id="@+id/top_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_alignParentTop="true">

        <TextView
            android:id="@+id/tv_coin_symbol"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="XRP"
            android:textColor="#AABBCC"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.12" />

        <TextView
            android:id="@+id/tv_change"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            android:textColor="#00FF88"
            android:layout_marginEnd="4dp"
            android:text="▲ 0.00%" />

        <ImageView
            android:id="@+id/btn_refresh"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="4dp"
            android:src="@drawable/widget_refresh_icon"
            android:contentDescription="Refresh" />
    </LinearLayout>

    <!-- BOTTOM ROW: last-updated timestamp + settings icon -->
    <LinearLayout
        android:id="@+id/bottom_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_alignParentBottom="true">

        <TextView
            android:id="@+id/tv_last_updated"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#5A5A88"
            android:textSize="10sp"
            android:fontFamily="sans-serif-light"
            android:text="Updating..." />

        <ImageView
            android:id="@+id/btn_settings"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="4dp"
            android:src="@drawable/widget_settings_icon"
            android:contentDescription="Settings" />
    </LinearLayout>

    <!-- SPARKLINE: above bottom row, with pulse overlay -->
    <FrameLayout
        android:id="@+id/sparkline_container"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_above="@id/bottom_row"
        android:layout_marginBottom="2dp">

        <ImageView
            android:id="@+id/iv_sparkline"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="Price chart" />

        <!-- Z=top: expanding pulse ring at the sparkline endpoint — theme-tinted at runtime -->
        <ImageView
            android:id="@+id/iv_sparkline_pulse"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:src="@drawable/sparkline_pulse_avd"
            android:contentDescription="@null" />
    </FrameLayout>

    <!-- PRICE ROW: price on the left, wallet balance stacked on the right -->
    <LinearLayout
        android:id="@+id/price_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_below="@id/top_row"
        android:layout_above="@id/sparkline_container"
        android:paddingTop="2dp"
        android:paddingBottom="2dp">

        <TextView
            android:id="@+id/tv_price"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#FFFFFF"
            android:textSize="30sp"
            android:fontFamily="sans-serif-medium"
            android:text="$0.0000" />

        <!-- Wallet: stacked column, right-aligned, hidden until balance is set -->
        <LinearLayout
            android:id="@+id/wallet_row"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end"
            android:visibility="gone">

            <TextView
                android:id="@+id/tv_balance"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#00D4FF"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium"
                android:text="0.0000 XRP" />

            <TextView
                android:id="@+id/tv_portfolio_value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#8899BB"
                android:textSize="12sp"
                android:fontFamily="sans-serif-light"
                android:text="≈ $0.00" />
        </LinearLayout>

    </LinearLayout>

</RelativeLayout>
